<% if (it.message) { %>
<p class="aviso aviso--sucesso" role="status"><%= it.message %></p>
<% } %>

<% if (it.error) { %>
<p class="aviso aviso--erro" role="alert"><%= it.error %></p>
<% } %>

<% if (Array.isArray(it.errors) && it.errors.length > 0) { %>
<div class="aviso aviso--erro" role="alert">
  <p class="aviso__titulo">Não foi possível concluir</p>
  <ul>
    <% it.errors.forEach((problem) => { %>
    <li><%= problem.mensagem %></li>
    <% }) %>
  </ul>
</div>
<% } %>
