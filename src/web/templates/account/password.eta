<%
const { errorFor, describedBy } = it;
const fields = it.fields.password;
%>
<div class="pagina__cabecalho">
  <div>
    <p class="sobretitulo">Conta</p>
    <h1 class="pagina__titulo"><%= it.title %></h1>
    <p class="pagina__resumo">
      Vale para qualquer papel. Depois de trocar, a senha antiga deixa de funcionar imediatamente;
      as sessões já abertas continuam válidas até expirarem.
    </p>
  </div>
</div>

<div class="quadro">
  <form class="formulario" method="post" action="<%= it.routes.account.password() %>">
    <input type="hidden" name="<%= it.keyField %>" value="<%= it.key %>">

    <div class="campo">
      <label class="campo__rotulo" for="senhaAtual">
        Senha atual <span class="campo__obrigatorio" aria-hidden="true">*</span>
      </label>
      <input
        id="senhaAtual"
        name="<%= fields.currentPassword %>"
        type="password"
        required
        autocomplete="current-password"
        <% if (errorFor(fields.currentPassword) !== '') { %>aria-invalid="true"<% } %>
        <% if (describedBy(fields.currentPassword, false) !== '') { %>aria-describedby="<%= describedBy(fields.currentPassword, false) %>"<% } %>>
      <% if (errorFor(fields.currentPassword) !== '') { %>
      <p class="campo__erro" id="<%= it.errorId(fields.currentPassword) %>"><%= errorFor(fields.currentPassword) %></p>
      <% } %>
    </div>

    <div class="campo">
      <label class="campo__rotulo" for="senhaNova">
        Senha nova <span class="campo__obrigatorio" aria-hidden="true">*</span>
      </label>
      <input
        id="senhaNova"
        name="<%= fields.newPassword %>"
        type="password"
        required
        autocomplete="new-password"
        <% if (errorFor(fields.newPassword) !== '') { %>aria-invalid="true"<% } %>
        aria-describedby="<%= describedBy(fields.newPassword, true) %>">
      <p class="campo__ajuda" id="<%= it.helpId(fields.newPassword) %>">
        Escolha uma senha longa e que você não use em outro sistema. Se ela for curta demais, o
        servidor recusa e diz o mínimo exigido.
      </p>
      <% if (errorFor(fields.newPassword) !== '') { %>
      <p class="campo__erro" id="<%= it.errorId(fields.newPassword) %>"><%= errorFor(fields.newPassword) %></p>
      <% } %>
    </div>

    <div class="campo">
      <label class="campo__rotulo" for="senhaConfirmacao">
        Repita a senha nova <span class="campo__obrigatorio" aria-hidden="true">*</span>
      </label>
      <input
        id="senhaConfirmacao"
        name="<%= fields.passwordConfirmation %>"
        type="password"
        required
        autocomplete="new-password"
        <% if (errorFor(fields.passwordConfirmation) !== '') { %>aria-invalid="true"<% } %>
        <% if (describedBy(fields.passwordConfirmation, false) !== '') { %>aria-describedby="<%= describedBy(fields.passwordConfirmation, false) %>"<% } %>>
      <% if (errorFor(fields.passwordConfirmation) !== '') { %>
      <p class="campo__erro" id="<%= it.errorId(fields.passwordConfirmation) %>"><%= errorFor(fields.passwordConfirmation) %></p>
      <% } %>
    </div>

    <div class="acoes">
      <button class="botao botao--primario" type="submit"><%= it.titles.changePassword %></button>
    </div>
  </form>
</div>
