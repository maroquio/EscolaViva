<%
const unidades = it.unidades ?? [];

const ROTULO_DA_SITUACAO = it.rotuloDaSituacao;
const SEM_VALOR = it.ausente;

const CLASSE_DA_SITUACAO = { sim: 'etiqueta etiqueta--ativa', nao: 'etiqueta' };
%>
<div class="pagina__cabecalho">
  <div>
    <p class="sobretitulo"><%= it.areas.rede %></p>
    <h1 class="pagina__titulo"><%= it.titulo %></h1>
    <p class="pagina__resumo">
      Cada escola da rede é uma unidade. Papéis, turmas e comunicados existem sempre dentro de
      uma — ninguém é "professor da rede".
    </p>
  </div>
  <div class="pagina__acoes">
    <a class="botao botao--primario" href="<%= it.rotas.rede.unidadeNova() %>"><%= it.titulos.rede.unidadeNova %></a>
  </div>
</div>

<section class="secao" aria-labelledby="lista-unidades-titulo">
  <div class="secao__cabecalho">
    <h2 id="lista-unidades-titulo">Unidades da rede</h2>
    <p class="secao__nota"><%= it.navegacao === undefined ? unidades.length : it.navegacao.total %> no total</p>
  </div>

  <% if (unidades.length === 0) { %>
    <%~ include(it.parciais.vazio, {
      titulo: 'Nenhuma unidade criada',
      texto: 'A primeira unidade abre o caminho para os usuários e as turmas.',
      acao: { href: it.rotas.rede.unidadeNova(), texto: it.titulos.rede.unidadeNova }
    }) %>
  <% } else { %>
  <div class="tabela-rolagem">
    <table class="tabela">
      <caption>Unidades cadastradas</caption>
      <thead>
        <tr>
          <th scope="col"><%= it.rotulos.unidade %></th>
          <th scope="col">Código INEP</th>
<% // magic-values: permitido — cabeça genérica de cinco vocabulários de estado distintos; não é o rótulo de um dado só, e por isso não entra em ROTULOS; nem é o `IDENTITY_VOCABULARY.schoolActive` da célula abaixo %>
          <th scope="col">Situação</th>
        </tr>
      </thead>
      <tbody>
        <% unidades.forEach((unidade) => { %>
        <tr>
          <th scope="row"><%= unidade.name %></th>
          <td class="numero"><%= unidade.inepCode === null ? SEM_VALOR : unidade.inepCode %></td>
          <td>
            <span class="<%= unidade.active ? CLASSE_DA_SITUACAO.sim : CLASSE_DA_SITUACAO.nao %>"><%= unidade.active ? ROTULO_DA_SITUACAO.yes : ROTULO_DA_SITUACAO.no %></span>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
  <%~ include(it.parciais.paginacao, { navegacao: it.navegacao, rotulo: it.contagem.unidade.plural }) %>
  <% } %>
</section>
