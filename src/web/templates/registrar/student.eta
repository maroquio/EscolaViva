<%
const student = it.student;
const guardianLinks = it.guardianLinks ?? [];
const enrollments = it.enrollments ?? [];
const active = it.active ?? null;
const separatorAtLineEnd = it.presentation.separator.trimEnd();

const STATUS_CLASS = it.tagClass.enrollmentStatus;
%>
<div class="pagina__cabecalho">
  <div>
    <p class="sobretitulo"><%= it.areas.registrar %><%= it.presentation.separator %><%= it.titles.registrar.student %></p>
    <h1 class="pagina__titulo"><%= student.name %></h1>
    <p class="pagina__resumo">
      Nascimento em <span class="numero"><%= it.formatDate(student.birthDate) %></span>.
      <% if (active === null) { %>
        Sem matrícula ativa nas suas unidades.
      <% } else { %>
        Matriculado na turma <%= active.classGroupName %>, ano letivo <span class="numero"><%= active.year %></span>.
      <% } %>
    </p>
  </div>
  <div class="pagina__acoes">
    <a class="botao botao--secundario" href="<%= it.routes.registrar.students() %>"><%= it.actions.backTo.search %></a>
  </div>
</div>

<section class="secao" aria-labelledby="data">
  <div class="secao__cabecalho">
    <h2 id="data">Dados do aluno</h2>
  </div>

  <div class="quadro">
    <dl class="lista-definicoes">
      <dt><%= it.labels.name %></dt>
      <dd><%= student.name %></dd>
      <dt>Nascimento</dt>
      <dd class="numero"><%= it.formatDate(student.birthDate) %></dd>
      <dt>Matrícula ativa</dt>
      <dd>
        <% if (active === null) { %>
        <span class="suave">nenhuma</span>
        <% } else { %>
        <span class="etiqueta etiqueta--ativa"><%= it.statusLabel[active.status] %></span>
        <%= active.classGroupName %><%= it.presentation.separator %>ano letivo <span class="numero"><%= active.year %></span><%= separatorAtLineEnd %>

        matriculado em <span class="numero"><%= it.formatDate(active.enrollmentDate) %></span>
        <% } %>
      </dd>
    </dl>
  </div>
</section>

<section class="secao" aria-labelledby="guardians">
  <div class="secao__cabecalho">
    <h2 id="guardians">Responsáveis vinculados</h2>
    <% const linkCount =
         it.guardianLinksPagination === undefined ? guardianLinks.length : it.guardianLinksPagination.total; %>
    <p class="secao__nota">
      <span class="numero"><%= linkCount %></span>
      <%= it.pluralize(linkCount, it.nouns.guardian) %>
    </p>
  </div>

  <div class="pilha pilha--larga">
    <% if (guardianLinks.length === 0) { %>
    <%~ include(it.partials.empty, {
      title: 'Nenhum responsável vinculado',
      text: 'O responsável é quem recebe comunicado e acompanha boletim e frequência deste aluno.',
      action: {
        href: it.routes.registrar.studentGuardianNew({ id: student.id }),
        text: it.titles.registrar.linkGuardian
      }
    }) %>
    <% } else { %>
    <div class="tabela-rolagem">
      <table class="tabela">
        <caption>Quem responde por este aluno</caption>
        <thead>
          <tr>
            <th scope="col"><%= it.labels.guardian %></th>
            <th scope="col">Parentesco</th>
            <th scope="col"><%= it.labels.email %></th>
            <th scope="col">Financeiro</th>
          </tr>
        </thead>
        <tbody>
          <% guardianLinks.forEach((link) => { %>
          <tr>
            <th scope="row"><%= link.name %></th>
            <td class="celula-texto"><%= link.relationship %></td>
            <td class="celula-texto"><%= link.email %></td>
            <td><%= link.financiallyResponsible ? 'Sim' : 'Não' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%~ include(it.partials.pagination, {
      pagination: it.guardianLinksPagination, label: 'responsáveis vinculados'
    }) %>

    <div class="acoes">
      <a class="botao botao--primario" href="<%= it.routes.registrar.studentGuardianNew({ id: student.id }) %>">
        <%= it.titles.registrar.linkGuardian %>

      </a>
    </div>
    <% } %>
  </div>
</section>

<section class="secao" aria-labelledby="enrollments">
  <div class="secao__cabecalho">
    <h2 id="enrollments">Matrículas</h2>
    <% const recordCount =
         it.enrollmentsPagination === undefined ? enrollments.length : it.enrollmentsPagination.total; %>
    <p class="secao__nota">
      <span class="numero"><%= recordCount %></span>
      <%= recordCount === 1 ? 'registro' : 'registros' %> nas suas unidades
    </p>
  </div>

  <div class="pilha pilha--larga">
    <% if (enrollments.length === 0) { %>
    <%~ include(it.partials.empty, {
      title: 'Nenhuma matrícula registrada',
      text: 'Este aluno ainda não foi matriculado em nenhuma turma sob sua secretaria.',
      action: { href: it.routes.registrar.studentEnroll({ id: student.id }), text: 'Matricular' }
    }) %>
    <% } else { %>
    <div class="tabela-rolagem">
      <table class="tabela">
        <caption>Histórico de matrículas deste aluno</caption>
        <thead>
          <tr>
            <th scope="col"><%= it.labels.classGroup %></th>
            <th scope="col"><%= it.labels.year %></th>
            <th scope="col">Data</th>
<% // magic-values: permitido — não é rótulo de LABELS, nem o ACADEMIC_VOCABULARY.enrollmentStatus da célula abaixo %>
            <th scope="col">Situação</th>
          </tr>
        </thead>
        <tbody>
          <% enrollments.forEach((enrollment) => { %>
          <tr>
            <th scope="row"><%= enrollment.classGroupName %></th>
            <td class="numero"><%= enrollment.year %></td>
            <td class="numero"><%= it.formatDate(enrollment.enrollmentDate) %></td>
            <td>
              <span class="etiqueta <%= STATUS_CLASS[enrollment.status] %>"><%= it.statusLabel[enrollment.status] %></span>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <%~ include(it.partials.pagination, {
      pagination: it.enrollmentsPagination, label: it.nouns.enrollment.plural
    }) %>

    <div class="acoes">
      <a class="botao botao--primario" href="<%= it.routes.registrar.studentEnroll({ id: student.id }) %>">
        <%= it.titles.registrar.enroll %>

      </a>
      <% if (active !== null) { %>
      <a class="botao botao--secundario" href="<%= it.routes.registrar.enrollmentTransfer({ id: active.id }) %>">
        <%= it.titles.registrar.transfer %>

      </a>
      <% } %>
      <p class="secao__nota">Um aluno tem, no máximo, uma matrícula ativa por ano letivo.</p>
    </div>
    <% } %>
  </div>
</section>
